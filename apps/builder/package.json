{
  "name": "@univocity-tools/builder",
  "version": "0.1.0",
  "private": true,
  "description": "CLI for Univocity contract build and deploy artifacts",
  "type": "module",
  "bin": {
    "builder": "./dist/cli.js"
  },
  "scripts": {
    "build": "bun build ./src/cli.ts --outdir dist --target bun",
    "build:binary": "bun ../../scripts/build-binary.ts --app builder",
    "build:binary:linux-x64": "bun ../../scripts/build-binary.ts --app builder --target bun-linux-x64",
    "build:binary:darwin-arm64": "bun ../../scripts/build-binary.ts --app builder --target bun-darwin-arm64",
    "dev": "bun run ./src/cli.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@univocity-tools/builder-common": "workspace:*",
    "citty": "^0.2.2"
  },
  "files": [
    "dist"
  ]
}
